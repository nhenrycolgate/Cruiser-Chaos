//have subscriptions for the game state as well as handle state changes
function GameController() {

    Controller.call(this, "GAME_CONTROLLER");

    this.direction = null;

    this.Init = function() {

    }

    this.GameOver = function() { //kills the current game

    }

}
